SPECIFICATION Spec

CONSTANTS
  MaxTimestep = 6
  Actions = {a0, a1}
  Payloads = {p0, p1}
  ActionsPerChunk = 3
  ScheduleMaxFactor = 2
  ChunkSizeThresholdNum = 1
  ChunkSizeThresholdDen = 2
  \* Network failure modeling: FALSE = atomic delivery, TRUE = may drop messages
  EnableNetworkFailures = FALSE
  \* Server timeout modeling: FALSE = always waits, TRUE = may timeout
  EnableServerTimeout = FALSE
  \* Server race condition: FALSE = no race, TRUE = enqueue can fail
  EnableServerEnqueueRace = FALSE

INVARIANT TypeOK
INVARIANT Inv_ObsSlotType
INVARIANT Inv_QueueOnlyFuture
INVARIANT Inv_ActionChunkSizeBound
INVARIANT Inv_AggregationProducesValidActions
INVARIANT Inv_NotDeadlocked

PROPERTY Live_IfQueueNonEmptyEventuallyExec
PROPERTY Live_MustGoObsEventuallyProducesChunk
